# 安全策略

## 支持范围

当前仅维护默认分支上的最新社区版。旧版本发现安全问题后，通常通过升级到最新版本修复。

## 报告漏洞

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。
在维护者确认问题前，请不要公开可直接复现的利用代码、真实凭据或受影响用户数据。

报告中建议包含：

- 受影响版本或提交号；
- 最小复现步骤；
- 可能影响；
- 已做的脱敏处理；
- 可选的修复建议。

不要提交真实 Cookie、Telegram Bot Token、翻译服务凭据、签名下载地址、完整日志或媒体文件。

## 安全边界

- 影迹是本地桌面工具，只监听 `127.0.0.1`，不应反向代理到局域网或公网。
- 下载 URL 会在专用 yt-dlp 子进程中检查初始请求和重定向；直连时逐次检查 DNS 结果。显式代理模式会强制目标流量经过代理，并由代理承担域名解析边界。
- 本地 HTTP API 使用随机会话 Cookie，并校验本机来源。
- Telegram 助手默认关闭，必须在本机配置 Token 和允许名单；建议每个 Bot 仅供自己使用。
- Cookie、Token 和任务数据只应保存在本机运行数据目录，不进入 Git、Issue 或发布包。

本策略不能替代操作系统隔离。处理不可信链接时，仍建议使用普通用户权限运行，不要以管理员身份启动。
